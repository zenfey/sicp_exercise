
; proof Fib(n) = (\phi^n - \gamma^n) / \sqrt{5}
; 
; hint: express fibnacci with a linear transformation A.
; then \{Fib(n), Fib(n-1)\} = A^n \dot \{Fib(1), Fib(0)}.
; the explicit form of Fib(n) is obtained via the explicit
; form of A^n, which is an easy exercise in linear algebra.